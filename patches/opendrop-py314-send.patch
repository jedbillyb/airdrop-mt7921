Only in /home/jed/owl/.venv-opendrop/lib/python3.14/site-packages/opendrop: certs
diff -ru '--exclude=__pycache__' sp/opendrop/client.py /home/jed/owl/.venv-opendrop/lib/python3.14/site-packages/opendrop/client.py
--- a/opendrop/client.py	2026-07-31 13:53:09.834619889 +1200
+++ b/opendrop/client.py	2026-07-31 13:50:31.006032393 +1200
@@ -24,6 +24,7 @@
 import platform
 import plistlib
 import socket
+import ssl
 from http.client import HTTPSConnection
 
 import fleep
@@ -263,15 +264,30 @@
         if timeout is None:
             timeout = socket.getdefaulttimeout()
 
+        # key_file, cert_file and check_hostname were deprecated on
+        # HTTPSConnection from Python 3.6 and REMOVED in 3.12, so forwarding them
+        # raises e.g.
+        #   TypeError: HTTPSConnection.__init__() got an unexpected keyword
+        #              argument 'key_file'
+        # and every send/find dies before a single packet goes out. All three now
+        # live on the SSLContext instead. AirDropConfig.get_ssl_context() already
+        # does the load_cert_chain(), so in normal use key_file/cert_file are None
+        # and there is nothing to carry over. They stay in the signature for
+        # callers that still pass them.
+        if context is None and (key_file or cert_file):
+            context = ssl.SSLContext(ssl.PROTOCOL_TLS_CLIENT)
+            context.check_hostname = False
+            context.verify_mode = ssl.CERT_NONE
+            context.load_cert_chain(cert_file, keyfile=key_file)
+        if check_hostname is not None and context is not None:
+            context.check_hostname = check_hostname
+
         super(HTTPSConnectionAWDL, self).__init__(
             host=host,
             port=port,
-            key_file=key_file,
-            cert_file=cert_file,
             timeout=timeout,
             source_address=source_address,
             context=context,
-            check_hostname=check_hostname,
         )
 
         self.interface_name = interface_name
Only in /home/jed/owl/.venv-opendrop/lib/python3.14/site-packages/opendrop: cli.py
Only in /home/jed/owl/.venv-opendrop/lib/python3.14/site-packages/opendrop: config.py
Only in /home/jed/owl/.venv-opendrop/lib/python3.14/site-packages/opendrop: __init__.py
Only in /home/jed/owl/.venv-opendrop/lib/python3.14/site-packages/opendrop: __main__.py
Only in /home/jed/owl/.venv-opendrop/lib/python3.14/site-packages/opendrop: server.py
diff -ru '--exclude=__pycache__' sp/opendrop/util.py /home/jed/owl/.venv-opendrop/lib/python3.14/site-packages/opendrop/util.py
--- a/opendrop/util.py	2026-07-31 13:53:09.834619889 +1200
+++ b/opendrop/util.py	2026-07-31 13:52:19.137766969 +1200
@@ -111,13 +111,17 @@
             pass  # no EXIF data available
 
         # Big image
-        im.thumbnail((540, 540), Image.ANTIALIAS)
+        # Image.ANTIALIAS was deprecated in Pillow 2.7 and REMOVED in 10.0;
+        # Image.Resampling.LANCZOS is the same filter under its current name.
+        # Without this, sending any image dies in send_ask() with
+        #   AttributeError: module 'PIL.Image' has no attribute 'ANTIALIAS'
+        im.thumbnail((540, 540), Image.Resampling.LANCZOS)
         img_bytes = io.BytesIO()
         im.save(img_bytes, format="JPEG2000")
         file_icon = img_bytes.getvalue()
 
         # Small image
-        # im.thumbnail((64, 64), Image.ANTIALIAS)
+        # im.thumbnail((64, 64), Image.Resampling.LANCZOS)
         # img_bytes = io.BytesIO()
         # im.save(img_bytes, format='JPEG2000')
         # small_file_icon = img_bytes.getvalue()
@@ -180,27 +184,37 @@
         if block_size <= 0:
             block_size = 10240  # pragma: no cover
 
-        with new_archive_entry() as entry_p:
-            entry = ArchiveEntry(None, entry_p)
-            with new_archive_read_disk(path) as read_p:
-                while True:
-                    r = read_next_header2(read_p, entry_p)
-                    if r == ARCHIVE_EOF:
-                        break
-                    entry.pathname = store_path
-                    read_disk_descend(read_p)
-                    write_header(write_p, entry_p)
-                    try:
-                        with open(entry_sourcepath(entry_p), "rb") as f:
-                            while True:
-                                data = f.read(block_size)
-                                if not data:
-                                    break
-                                write_data(write_p, data, len(data))
-                    except IOError as e:
-                        if e.errno != 21:
-                            raise  # pragma: no cover
-                    write_finish_entry(write_p)
-                    entry_clear(entry_p)
-                    if os.path.isdir(path):
-                        break
+        # libarchive-c changed ArchiveEntry's signature: the second positional
+        # argument is now `header_codec` (a str), not an entry pointer, and the
+        # entry allocates its own C struct in __init__. The old call
+        # `ArchiveEntry(None, entry_p)` therefore stuffed a pointer into
+        # header_codec and blew up on the first pathname assignment with
+        #   TypeError: encode() argument 'encoding' must be str, not int
+        # Let the ArchiveEntry own its struct and use that pointer throughout.
+        # This deliberately drops the old `new_archive_entry()` context manager:
+        # it would free the same struct that ArchiveEntry.__del__ frees, which is
+        # a double free.
+        entry = ArchiveEntry(None, "utf-8")
+        entry_p = entry._entry_p
+        with new_archive_read_disk(path) as read_p:
+            while True:
+                r = read_next_header2(read_p, entry_p)
+                if r == ARCHIVE_EOF:
+                    break
+                entry.pathname = store_path
+                read_disk_descend(read_p)
+                write_header(write_p, entry_p)
+                try:
+                    with open(entry_sourcepath(entry_p), "rb") as f:
+                        while True:
+                            data = f.read(block_size)
+                            if not data:
+                                break
+                            write_data(write_p, data, len(data))
+                except IOError as e:
+                    if e.errno != 21:
+                        raise  # pragma: no cover
+                write_finish_entry(write_p)
+                entry_clear(entry_p)
+                if os.path.isdir(path):
+                    break
